{
  "mcpServers": {
    "docmost": {
      "type": "http",
      "url": "$MCP_HOST/mcp",
      "headers": {
        "X-Docmost-Url": "${DOCMOST_URL}",
        "X-Docmost-Email": "${DOCMOST_EMAIL}",
        "X-Docmost-Password": "${DOCMOST_PASSWORD}"
      }
    }
  }
}